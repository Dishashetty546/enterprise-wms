/**
 * List of possible task types in the project management system.
 *
 */
export const TASK_TYPES = ['Bug', 'Feature', 'Improvement']

/**
 * Priority levels that can be assigned to tasks.
 *
 */
export const PRIORITIES = ['Low', 'Medium', 'High']

/**
 * Example usage:
 * const newTask = {
 *   title: 'Fix login bug',
 *   type: TASK_TYPES[0], // 'Bug'
 *   priority: PRIORITIES[2], // 'High'
 * };
 */
